// Use push to add new element at the end of the array.

var numbers = [78, 52, 60, 60, 800];
// console.log(numbers)
numbers.push(90);
console.log(numbers);


// Use pop to remove the last element of the array.
numbers.pop();
console.log(numbers);



// Make the pop element into a new variable .

var element = numbers.pop();
console.log(element);
console.log(numbers);