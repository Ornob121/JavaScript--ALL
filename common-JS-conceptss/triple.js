const first = 2; 
const second = '2';

// Triple equal(===) check the value with type while double equal just check value. so i should always use triple equal(===);

// Equal comparison does not work for non-primitive type;

if(first === second){
    console.log('values are equal')
}
else{
    console.log('values are not equal');
}